#pragma once

#include <iostream>
#include <string>
#include <regex>

class Utils
{
public:
	static char getCharacter();
	static std::string readLine();
	static bool validateInput(std::string input);
};

